# Cloudflare Pages 部署教學

本文件提供完整的步驟說明,協助您將 Aeonix Technology Limited 網站部署到 Cloudflare Pages。

---

## 目錄

1. [前置準備](#前置準備)
2. [建置網站檔案](#建置網站檔案)
3. [上傳至 GitHub](#上傳至-github)
4. [連接 Cloudflare Pages](#連接-cloudflare-pages)
5. [設定自訂網域](#設定自訂網域)
6. [常見問題](#常見問題)

---

## 前置準備

在開始部署之前,請確保您已準備以下項目:

### 必要帳號

- **GitHub 帳號**: 用於託管原始碼 (免費註冊: https://github.com/signup)
- **Cloudflare 帳號**: 用於部署網站 (免費註冊: https://dash.cloudflare.com/sign-up)

### 本地環境

如果您需要在本地測試網站,請確保已安裝:

- **Node.js** (版本 18 或以上): https://nodejs.org/
- **pnpm** (套件管理工具): 安裝 Node.js 後執行 `npm install -g pnpm`

---

## 建置網站檔案

### 步驟 1: 安裝相依套件

在專案根目錄執行以下指令:

```bash
pnpm install
```

### 步驟 2: 建置生產版本

執行建置指令,生成靜態網站檔案:

```bash
pnpm build
```

建置完成後,所有靜態檔案會輸出到 `dist/public` 目錄。

### 步驟 3: 本地預覽 (選用)

如果想在本地預覽建置後的網站:

```bash
pnpm preview
```

網站將在 `http://localhost:4173` 開啟。

---

## 上傳至 GitHub

### 方法 A: 使用 GitHub Desktop (推薦給初學者)

**步驟 1: 下載並安裝 GitHub Desktop**

前往 https://desktop.github.com/ 下載並安裝。

**步驟 2: 建立新的 Repository**

1. 開啟 GitHub Desktop
2. 點選 **File** → **New Repository**
3. 填寫以下資訊:
   - **Name**: `aeonix-gpu-website`
   - **Local Path**: 選擇專案資料夾的上一層目錄
   - **Initialize with README**: 不勾選 (專案已有 README)
4. 點選 **Create Repository**

**步驟 3: 發布到 GitHub**

1. 點選右上角的 **Publish repository**
2. 確認 Repository 名稱
3. 取消勾選 **Keep this code private** (如果想公開) 或保持勾選 (私有)
4. 點選 **Publish Repository**

### 方法 B: 使用 Git 指令列

**步驟 1: 初始化 Git Repository**

在專案根目錄執行:

```bash
git init
git add .
git commit -m "Initial commit: Aeonix GPU website"
```

**步驟 2: 建立 GitHub Repository**

1. 前往 https://github.com/new
2. Repository name 填寫: `aeonix-gpu-website`
3. 選擇 Public 或 Private
4. **不要** 勾選 "Initialize this repository with a README"
5. 點選 **Create repository**

**步驟 3: 推送到 GitHub**

將 GitHub 提供的指令中的 `<your-username>` 替換為您的 GitHub 使用者名稱:

```bash
git remote add origin https://github.com/<your-username>/aeonix-gpu-website.git
git branch -M main
git push -u origin main
```

---

## 連接 Cloudflare Pages

### 步驟 1: 登入 Cloudflare Dashboard

前往 https://dash.cloudflare.com/ 並登入您的帳號。

### 步驟 2: 建立新的 Pages 專案

1. 在左側選單選擇 **Workers & Pages**
2. 點選右上角的 **Create application**
3. 選擇 **Pages** 標籤
4. 點選 **Connect to Git**

### 步驟 3: 授權 GitHub 存取

1. 選擇 **GitHub** 作為 Git 提供者
2. 點選 **Connect GitHub**
3. 在彈出視窗中授權 Cloudflare 存取您的 GitHub 帳號
4. 選擇要授權的 Repository:
   - **All repositories** (所有專案) 或
   - **Only select repositories** (僅選擇 `aeonix-gpu-website`)

### 步驟 4: 設定建置配置

選擇 `aeonix-gpu-website` Repository 後,填寫以下設定:

| 設定項目 | 值 |
|---------|-----|
| **Project name** | `aeonix-gpu-website` (可自訂) |
| **Production branch** | `main` |
| **Framework preset** | `Vite` |
| **Build command** | `pnpm build` |
| **Build output directory** | `dist/public` |
| **Root directory** | `/` (保持空白或填 `/`) |

### 步驟 5: 環境變數 (選用)

本專案為靜態網站,不需要額外的環境變數。如果未來需要,可在此處新增。

### 步驟 6: 開始部署

點選 **Save and Deploy** 按鈕,Cloudflare 將開始建置和部署您的網站。

### 步驟 7: 等待部署完成

部署過程通常需要 1-3 分鐘。您可以在部署日誌中查看進度。

部署成功後,您會看到:

- ✅ **部署成功** 訊息
- 🌐 **網站網址**: `https://aeonix-gpu-website.pages.dev` (實際網址會根據專案名稱而定)

---

## 設定自訂網域

如果您擁有自己的網域 (例如 `aeonix.tech`),可以將其連接到 Cloudflare Pages。

### 步驟 1: 新增自訂網域

1. 在 Cloudflare Pages 專案頁面,點選 **Custom domains** 標籤
2. 點選 **Set up a custom domain**
3. 輸入您的網域名稱 (例如 `www.aeonix.tech` 或 `aeonix.tech`)
4. 點選 **Continue**

### 步驟 2: 設定 DNS 記錄

Cloudflare 會自動偵測您的網域是否已在 Cloudflare 管理。

**情況 A: 網域已在 Cloudflare**

Cloudflare 會自動新增必要的 DNS 記錄,您只需點選 **Activate domain** 即可。

**情況 B: 網域在其他 DNS 提供商**

您需要在您的 DNS 提供商 (例如 GoDaddy, Namecheap) 新增以下記錄:

| 類型 | 名稱 | 目標 |
|------|------|------|
| **CNAME** | `www` | `aeonix-gpu-website.pages.dev` |
| **CNAME** | `@` 或根網域 | `aeonix-gpu-website.pages.dev` |

**注意**: 有些 DNS 提供商不支援根網域的 CNAME 記錄,此時可使用 A 記錄指向 Cloudflare 的 IP 位址 (Cloudflare 會在設定頁面提供)。

### 步驟 3: 等待 DNS 生效

DNS 記錄更新通常需要 5 分鐘至 48 小時。您可以使用以下工具檢查:

- https://dnschecker.org/

### 步驟 4: 啟用 HTTPS

Cloudflare 會自動為您的自訂網域提供免費的 SSL/TLS 憑證,確保網站使用 HTTPS 加密連線。

---

## 常見問題

### Q1: 部署失敗怎麼辦?

**檢查建置日誌**

在 Cloudflare Pages 的部署頁面,點選失敗的部署記錄,查看詳細的錯誤訊息。

**常見錯誤:**

1. **Build command 錯誤**: 確認 Build command 設定為 `pnpm build`
2. **Build output directory 錯誤**: 確認設定為 `dist/public`
3. **Node.js 版本不符**: 在專案根目錄新增 `.nvmrc` 檔案,內容為 `18` 或更高版本

### Q2: 如何更新網站內容?

每次您將變更推送到 GitHub 的 `main` 分支,Cloudflare Pages 會自動觸發新的部署。

**使用 GitHub Desktop:**

1. 修改檔案後,開啟 GitHub Desktop
2. 在左下角填寫 Commit 訊息
3. 點選 **Commit to main**
4. 點選右上角的 **Push origin**

**使用 Git 指令:**

```bash
git add .
git commit -m "Update website content"
git push origin main
```

### Q3: 如何回復到先前的版本?

1. 在 Cloudflare Pages 專案頁面,點選 **Deployments** 標籤
2. 找到您想回復的部署版本
3. 點選該版本右側的 **⋯** 選單
4. 選擇 **Rollback to this deployment**

### Q4: 網站載入速度慢怎麼辦?

Cloudflare Pages 已內建全球 CDN,通常載入速度非常快。如果遇到問題:

1. **檢查圖片大小**: 確保圖片已壓縮 (建議使用 WebP 格式)
2. **啟用 Cloudflare 優化**: 在 Cloudflare Dashboard 的 **Speed** 設定中啟用 Auto Minify 和 Brotli 壓縮
3. **檢查網路連線**: 使用 https://www.speedtest.net/ 測試您的網路速度

### Q5: 如何查看網站流量統計?

Cloudflare Pages 提供基本的流量統計:

1. 在專案頁面點選 **Analytics** 標籤
2. 查看訪客數、頁面瀏覽量、頻寬使用等資訊

如需更詳細的分析,建議整合 Google Analytics 或其他分析工具。

### Q6: Cloudflare Pages 免費方案有什麼限制?

**免費方案包含:**

- ✅ 無限網站數量
- ✅ 無限請求次數
- ✅ 無限頻寬
- ✅ 每月 500 次建置
- ✅ 每次建置最多 20 分鐘
- ✅ 免費 SSL/TLS 憑證
- ✅ 全球 CDN

對於大多數中小型網站,免費方案已經足夠使用。

---

## 技術支援

如果您在部署過程中遇到任何問題,可以參考以下資源:

- **Cloudflare Pages 官方文件**: https://developers.cloudflare.com/pages/
- **Cloudflare 社群論壇**: https://community.cloudflare.com/
- **GitHub 支援**: https://support.github.com/

---

## 總結

完成以上步驟後,您的 Aeonix Technology Limited 網站已成功部署到 Cloudflare Pages,並可透過全球 CDN 快速存取。網站支援繁體中文與英文雙語切換,提供專業的顯示卡收購與銷售服務展示。

**部署後的優勢:**

- 🚀 **高效能**: 透過 Cloudflare 全球 CDN 加速
- 🔒 **安全**: 自動提供 SSL/TLS 加密
- 💰 **免費**: 完全免費的託管服務
- 🔄 **自動部署**: 推送到 GitHub 即自動更新
- 📊 **流量分析**: 內建基本分析功能

祝您部署順利!
