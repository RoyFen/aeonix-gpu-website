# Aeonix Technology Limited - 顯示卡收購與銷售網站

專業的雙語一頁式網站,提供顯示卡收購、新品與二手銷售、專業諮詢服務。

![Website Preview](./preview.png)

---

## 專案特色

### 🌐 雙語支援
- **繁體中文** (預設)
- **English**
- 語言偏好自動儲存至瀏覽器

### 🎨 現代化設計
- 深色科技風格主題
- 流暢的動畫效果
- 響應式設計,支援各種裝置

### ⚡ 高效能
- React 19 + Vite 打造
- 靜態網站生成 (SSG)
- 優化的圖片載入

### 📱 完整功能
- 導航列與錨點滾動
- 服務介紹區塊
- 產品展示 (NVIDIA & AMD)
- 優勢說明
- 聯絡表單
- 社交媒體連結

---

## 技術堆疊

| 技術 | 版本 | 用途 |
|------|------|------|
| **React** | 19.0.0 | UI 框架 |
| **TypeScript** | 5.6.3 | 型別安全 |
| **Vite** | 7.1.7 | 建置工具 |
| **Tailwind CSS** | 4.1.14 | 樣式框架 |
| **shadcn/ui** | Latest | UI 元件庫 |
| **Wouter** | 3.3.5 | 輕量路由 |
| **Lucide React** | 0.453.0 | 圖示庫 |

---

## 快速開始

### 前置需求

- **Node.js** 18 或以上
- **pnpm** (推薦) 或 npm

### 安裝相依套件

```bash
pnpm install
```

### 開發模式

啟動開發伺服器 (支援熱重載):

```bash
pnpm dev
```

網站將在 `http://localhost:3000` 開啟。

### 建置生產版本

```bash
pnpm build
```

建置後的檔案位於 `dist/public` 目錄。

### 預覽生產版本

```bash
pnpm preview
```

---

## 專案結構

```
aeonix-gpu-website/
├── client/                      # 前端原始碼
│   ├── public/                  # 靜態資源
│   │   ├── hero-gpu.png        # Hero 區塊背景圖
│   │   ├── service-buy.png     # 收購服務圖片
│   │   ├── service-sell.png    # 銷售服務圖片
│   │   ├── service-consult.png # 諮詢服務圖片
│   │   └── gpu-collection.png  # 產品展示背景圖
│   ├── src/
│   │   ├── components/         # React 元件
│   │   │   ├── Navbar.tsx     # 導航列
│   │   │   ├── Hero.tsx       # 主視覺區塊
│   │   │   ├── Services.tsx   # 服務介紹
│   │   │   ├── Products.tsx   # 產品展示
│   │   │   ├── WhyUs.tsx      # 優勢說明
│   │   │   ├── Contact.tsx    # 聯絡表單
│   │   │   ├── Footer.tsx     # 頁尾
│   │   │   └── ui/            # shadcn/ui 元件
│   │   ├── contexts/          # React Context
│   │   │   ├── LanguageContext.tsx  # 語言切換
│   │   │   └── ThemeContext.tsx     # 主題管理
│   │   ├── lib/               # 工具函式
│   │   │   └── translations.ts      # 翻譯內容
│   │   ├── pages/             # 頁面元件
│   │   │   └── Home.tsx       # 首頁
│   │   ├── App.tsx            # 應用程式入口
│   │   ├── main.tsx           # React 入口
│   │   └── index.css          # 全域樣式
│   └── index.html             # HTML 模板
├── server/                     # 伺服器 (靜態專案僅用於開發)
├── CLOUDFLARE_DEPLOYMENT.md   # Cloudflare Pages 部署教學
├── package.json               # 專案設定
└── README.md                  # 本文件
```

---

## 自訂內容

### 修改公司資訊

編輯 `client/src/lib/translations.ts` 檔案,更新以下內容:

```typescript
export const translations: Record<Language, Translations> = {
  zh: {
    contact: {
      emailInfo: "電子郵件",
      phoneInfo: "聯絡電話",
      addressInfo: "地址",
      // ... 其他內容
    }
  }
}
```

### 更換圖片

將新圖片放置於 `client/public/` 目錄,並更新元件中的圖片路徑:

```tsx
// 例如在 Hero.tsx 中
<img src="/your-new-image.png" alt="Description" />
```

### 修改色彩主題

編輯 `client/src/index.css` 檔案中的 CSS 變數:

```css
:root {
  --primary: oklch(0.65 0.25 220);  /* 主要色 */
  --accent: oklch(0.70 0.30 150);   /* 強調色 */
  --background: oklch(0.10 0.02 240); /* 背景色 */
  /* ... 其他顏色 */
}
```

---

## 部署

### Cloudflare Pages (推薦)

詳細步驟請參考 [CLOUDFLARE_DEPLOYMENT.md](./CLOUDFLARE_DEPLOYMENT.md)。

**快速摘要:**

1. 將專案推送到 GitHub
2. 登入 Cloudflare Dashboard
3. 建立新的 Pages 專案並連接 GitHub
4. 設定建置指令:
   - Build command: `pnpm build`
   - Build output directory: `dist/public`
5. 部署完成!

### 其他平台

本專案為標準的 Vite 靜態網站,也可部署到:

- **Vercel**: 連接 GitHub Repository,自動偵測設定
- **Netlify**: 上傳 `dist/public` 目錄
- **GitHub Pages**: 使用 GitHub Actions 自動部署
- **傳統主機**: 上傳 `dist/public` 目錄內容至網站根目錄

---

## 瀏覽器支援

| 瀏覽器 | 最低版本 |
|--------|----------|
| Chrome | 90+ |
| Firefox | 88+ |
| Safari | 14+ |
| Edge | 90+ |

---

## 授權

本專案由 Manus AI 建立,供 Aeonix Technology Limited 使用。

---

## 聯絡資訊

**Aeonix Technology Limited**

- 📧 Email: sales@info.dong-win.com
- 📍 Address: FLAT/RM709, 7/F, FU HANG INDUSTRIAL BUILDING NO.1 HOK YUEN STREET EAST HUNGHOM HK
- 🌐 Website: https://aeonix-gpu-website.pages.dev

---

## 更新日誌

### v1.0.0 (2024-11-29)

- ✨ 初始版本發布
- 🌐 支援繁體中文與英文雙語
- 🎨 深色科技風格設計
- 📱 完整響應式支援
- ⚡ 優化效能與載入速度
